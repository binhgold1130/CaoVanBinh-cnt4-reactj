@model IEnumerable<Cvblesson06.Models.CvbSong>

@{
    ViewBag.Title = "Cvb:Danh sách bài hát";
}

<h2>Cvb:Danh sách bài hát </h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CvbTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CvbAuthor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CvbArtist)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CvbYearRelease)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CvbTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CvbAuthor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CvbArtist)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CvbYearRelease)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
